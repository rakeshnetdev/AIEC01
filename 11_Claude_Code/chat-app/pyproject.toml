[project]
name = "codebase-concierge"
version = "0.1.0"
description = "Session 11 chat app: a codebase concierge powered by the Claude Agent SDK."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "claude-agent-sdk>=0.1.0",
    "fastapi>=0.115.0",
    "python-dotenv>=1.0.0",
    "uvicorn[standard]>=0.34.0",
]

[tool.uv]
package = false
